{
  "name": "sticker-collector",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@8.5.1",
  "scripts": {
    "dev": "wrangler dev",
    "build": "pnpm -r --filter=./packages/web run build",
    "test": "vitest run",
    "typecheck": "tsc -b",
    "lint": "biome ci ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^4.0.0"
  }
}
